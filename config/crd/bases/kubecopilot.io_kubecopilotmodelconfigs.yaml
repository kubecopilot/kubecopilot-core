---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kubecopilotmodelconfigs.kubecopilot.io
spec:
  group: kubecopilot.io
  names:
    kind: KubeCopilotModelConfig
    listKind: KubeCopilotModelConfigList
    plural: kubecopilotmodelconfigs
    singular: kubecopilotmodelconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.endpoint
      name: Endpoint
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          KubeCopilotModelConfig is the Schema for the kubecopilotmodelconfigs API.
          It represents a centralized LLM provider configuration including credentials,
          available models, rate limits, and fallback chains.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubeCopilotModelConfigSpec defines the desired state of KubeCopilotModelConfig.
            properties:
              endpoint:
                description: |-
                  Endpoint is the base URL for the provider API.
                  Leave empty to use the provider's default public endpoint.
                type: string
              fallback:
                description: |-
                  Fallback optionally references another KubeCopilotModelConfig to try
                  when this provider fails or is rate-limited.
                properties:
                  name:
                    description: Name of the referent.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              models:
                description: Models is the list of LLM models available through this
                  provider.
                items:
                  description: ModelEntry describes a single LLM model available from
                    a provider.
                  properties:
                    costPerInputToken:
                      description: |-
                        CostPerInputToken is the cost in USD per input token expressed as a
                        decimal string (e.g. "0.000001") for cost-based routing decisions.
                      type: string
                    costPerOutputToken:
                      description: |-
                        CostPerOutputToken is the cost in USD per output token expressed as a
                        decimal string (e.g. "0.000002") for cost-based routing decisions.
                      type: string
                    maxTokens:
                      description: MaxTokens is the maximum context window size for
                        this model.
                      format: int32
                      minimum: 1
                      type: integer
                    name:
                      description: Name is the model identifier (e.g. "gpt-4o", "claude-sonnet-4").
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              provider:
                description: Provider is the LLM provider type.
                enum:
                - openai
                - azure-openai
                - anthropic
                - google-vertex
                - ollama
                - custom
                type: string
              rateLimits:
                description: RateLimits configures request and token rate limits for
                  this provider.
                properties:
                  requestsPerMinute:
                    description: RequestsPerMinute is the maximum number of API requests
                      per minute.
                    format: int32
                    minimum: 1
                    type: integer
                  tokensPerMinute:
                    description: TokensPerMinute is the maximum number of tokens per
                      minute.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              secretRef:
                description: |-
                  SecretRef references a Kubernetes Secret that contains the API key
                  under the key "api-key". The Secret must be in the same namespace.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - provider
            type: object
          status:
            description: KubeCopilotModelConfigStatus defines the observed state of
              KubeCopilotModelConfig.
            properties:
              conditions:
                description: Conditions represent the current state of the model config.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastValidated:
                description: LastValidated is the timestamp of the last successful
                  provider connectivity check.
                format: date-time
                type: string
              phase:
                description: 'Phase describes the current lifecycle phase: Available,
                  Unavailable, Error.'
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
