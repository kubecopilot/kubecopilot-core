{{- if .Values.installCRDs }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kubecopilotcancels.kubecopilot.io
spec:
  group: kubecopilot.io
  names:
    kind: KubeCopilotCancel
    listKind: KubeCopilotCancelList
    plural: kubecopilotcancels
    singular: kubecopilotcancel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.sendRef
      name: SendRef
      type: string
    - jsonPath: .spec.agentRef
      name: AgentRef
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: KubeCopilotCancel cancels an in-flight KubeCopilotSend request.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubeCopilotCancelSpec defines the desired state of KubeCopilotCancel
            properties:
              agentRef:
                description: AgentRef is the name of the KubeCopilotAgent in the same
                  namespace.
                type: string
              sendRef:
                description: SendRef is the name of the KubeCopilotSend to cancel.
                type: string
            required:
            - agentRef
            - sendRef
            type: object
          status:
            description: KubeCopilotCancelStatus defines the observed state of KubeCopilotCancel.
            properties:
              errorMessage:
                description: ErrorMessage contains error details when Phase is Error.
                type: string
              phase:
                description: 'Phase: Pending, Cancelled, Error.'
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
