---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kubecopilottoolservers.kubecopilot.io
spec:
  group: kubecopilot.io
  names:
    kind: KubeCopilotToolServer
    listKind: KubeCopilotToolServerList
    plural: kubecopilottoolservers
    singular: kubecopilottoolserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.url
      name: URL
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          KubeCopilotToolServer is the Schema for the kubecopilottoolservers API.
          It represents an MCP (Model Context Protocol) server endpoint that provides
          tools to KubeCopilotAgent instances.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubeCopilotToolServerSpec defines the desired state of KubeCopilotToolServer.
            properties:
              headers:
                additionalProperties:
                  type: string
                description: Headers is optional authentication headers to send with
                  requests.
                type: object
              secretRef:
                description: |-
                  SecretRef optionally references a Secret containing authentication credentials.
                  The Secret data keys are added as HTTP headers on MCP requests.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
              transport:
                default: sse
                description: 'Transport is the MCP transport type: "sse" or "streamable-http".
                  Defaults to "sse".'
                enum:
                - sse
                - streamable-http
                type: string
              url:
                description: URL is the endpoint URL of the MCP server (e.g., "http://mcp-k8s:8080/sse").
                minLength: 1
                type: string
            required:
            - url
            type: object
          status:
            description: KubeCopilotToolServerStatus defines the observed state of
              KubeCopilotToolServer.
            properties:
              availableTools:
                description: AvailableTools lists the tool names discovered from the
                  MCP server.
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the current state of the tool server.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastChecked:
                description: LastChecked is the timestamp of the last health check.
                format: date-time
                type: string
              phase:
                description: 'Phase describes the current lifecycle phase: Available,
                  Unavailable, Error.'
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
