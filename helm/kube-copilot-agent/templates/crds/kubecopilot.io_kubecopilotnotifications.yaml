{{- if .Values.installCRDs }}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kubecopilotnotifications.kubecopilot.io
spec:
  group: kubecopilot.io
  names:
    kind: KubeCopilotNotification
    listKind: KubeCopilotNotificationList
    plural: kubecopilotnotifications
    singular: kubecopilotnotification
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.agentRef
      name: AgentRef
      type: string
    - jsonPath: .spec.sessionID
      name: SessionID
      type: string
    - jsonPath: .spec.notificationType
      name: Type
      type: string
    - jsonPath: .spec.title
      name: Title
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          KubeCopilotNotification is the Schema for the kubecopilotnotifications API.
          It represents a one-way message from the agent to the user session,
          typically generated by background tasks such as resource monitoring.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              KubeCopilotNotificationSpec defines the content of a one-way notification
              from the agent to the user session, typically triggered by background tasks.
            properties:
              agentRef:
                description: AgentRef is the name of the KubeCopilotAgent that produced
                  this notification.
                type: string
              message:
                description: Message is the notification body (supports markdown).
                type: string
              notificationType:
                default: info
                description: 'NotificationType categorises the notification: info,
                  success, warning, error.'
                enum:
                - info
                - success
                - warning
                - error
                type: string
              sessionID:
                description: SessionID is the conversation session this notification
                  belongs to.
                type: string
              taskRef:
                description: TaskRef is an optional reference to the background task
                  that generated this notification.
                type: string
              title:
                description: Title is an optional short summary shown in toasts.
                type: string
            required:
            - agentRef
            - message
            - sessionID
            type: object
          status:
            description: KubeCopilotNotificationStatus defines the observed state
              of KubeCopilotNotification.
            properties:
              createdAt:
                description: CreatedAt is the timestamp when this notification was
                  recorded.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
