---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kubecopilotsends.kubecopilot.io
spec:
  group: kubecopilot.io
  names:
    kind: KubeCopilotSend
    listKind: KubeCopilotSendList
    plural: kubecopilotsends
    singular: kubecopilotsend
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.agentRef
      name: AgentRef
      type: string
    - jsonPath: .status.queueID
      name: QueueID
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: KubeCopilotSend is the Schema for the kubecopilotsends API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubeCopilotSendSpec defines the desired state of KubeCopilotSend
            properties:
              agentRef:
                description: AgentRef is the name of the KubeCopilotAgent in the same
                  namespace.
                type: string
              message:
                description: Message is the prompt to send to the copilot agent asynchronously.
                type: string
              sessionConfig:
                description: SessionConfig holds optional per-session configuration
                  overrides.
                properties:
                  customAgents:
                    description: CustomAgents defines sub-agents available in this
                      session.
                    items:
                      description: CustomAgent defines a sub-agent that the copilot
                        can delegate to.
                      properties:
                        description:
                          description: Description explains what this agent does.
                          type: string
                        displayName:
                          description: DisplayName is the human-readable name shown
                            in UI.
                          type: string
                        infer:
                          description: Infer controls whether the agent infers tool
                            use. Defaults to true.
                          type: boolean
                        name:
                          description: Name is the unique identifier of the custom
                            agent.
                          type: string
                        prompt:
                          description: Prompt is the system prompt for this sub-agent.
                          type: string
                        tools:
                          description: Tools is the list of tool names this agent
                            can use.
                          items:
                            type: string
                          type: array
                      required:
                      - name
                      - prompt
                      type: object
                    type: array
                  disabledSkills:
                    description: DisabledSkills is a list of skill names to disable
                      for this session.
                    items:
                      type: string
                    type: array
                  model:
                    description: Model overrides the default model (e.g. "gpt-4o",
                      "claude-sonnet-4").
                    type: string
                  provider:
                    description: Provider specifies a BYOK model provider for this
                      session.
                    properties:
                      baseURL:
                        description: BaseURL is the base URL of the model provider's
                          API.
                        type: string
                      secretRef:
                        description: 'SecretRef references a Secret containing the
                          API key (key: "api-key").'
                        type: string
                      type:
                        description: Type is the provider type (e.g. "openai", "azure").
                        type: string
                    type: object
                  systemMessage:
                    description: SystemMessage overrides the default system prompt.
                    type: string
                  toolsConfig:
                    additionalProperties:
                      type: boolean
                    description: ToolsConfig enables or disables specific tools by
                      name.
                    type: object
                type: object
              sessionID:
                description: SessionID optionally continues an existing conversation
                  session.
                type: string
            required:
            - agentRef
            - message
            type: object
          status:
            description: KubeCopilotSendStatus defines the observed state of KubeCopilotSend.
            properties:
              conditions:
                description: Conditions represent the current state of the send request.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              errorMessage:
                description: ErrorMessage contains error details when Phase is Error.
                type: string
              phase:
                description: 'Phase: Pending, Queued, Done, Error.'
                type: string
              queueID:
                description: QueueID is the identifier returned by the agent for this
                  queued request.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
