---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kubecopilotagents.kubecopilot.io
spec:
  group: kubecopilot.io
  names:
    kind: KubeCopilotAgent
    listKind: KubeCopilotAgentList
    plural: kubecopilotagents
    singular: kubecopilotagent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.agentID
      name: AgentID
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: KubeCopilotAgent is the Schema for the kubecopilotagents API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubeCopilotAgentSpec defines the desired state of KubeCopilotAgent
            properties:
              agentConfigMap:
                description: |-
                  AgentConfigMap is the name of a ConfigMap that must contain a key
                  "AGENT.md" mounted at /home/copilot/.copilot/AGENT.md.
                type: string
              githubTokenSecretRef:
                description: GitHubTokenSecretRef references a Secret containing key
                  GITHUB_TOKEN.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
              image:
                description: Image overrides the default agent container image.
                type: string
              kubeconfigSecretRef:
                description: |-
                  KubeconfigSecretRef optionally references a Secret containing a kubeconfig
                  under key "config". When set, the secret is mounted at /copilot/.kube/config
                  and KUBECONFIG is set accordingly, giving kubectl/oc access to that cluster.
                  This field is mutually exclusive with RBAC — when RBAC is configured the
                  operator generates the kubeconfig automatically.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
              rbac:
                description: |-
                  RBAC configures a ServiceAccount and role-based permissions for the agent.
                  When set, the operator creates a ServiceAccount, a Role with the specified
                  rules, a RoleBinding, and a kubeconfig Secret so the agent pod runs with
                  least-privilege access scoped to these permissions.
                  This field is mutually exclusive with KubeconfigSecretRef.
                properties:
                  clusterRules:
                    description: |-
                      ClusterRules define cluster-scoped permissions granted to the agent's
                      ServiceAccount via a ClusterRole and ClusterRoleBinding.
                    items:
                      description: |-
                        PolicyRule holds information that describes a policy rule, but does not contain information
                        about who the rule applies to or which namespace the rule applies to.
                      properties:
                        apiGroups:
                          description: |-
                            APIGroups is the name of the APIGroup that contains the resources.  If multiple API groups are specified, any action requested against one of
                            the enumerated resources in any API group will be allowed. "" represents the core API group and "*" represents all API groups.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        nonResourceURLs:
                          description: |-
                            NonResourceURLs is a set of partial urls that a user should have access to.  *s are allowed, but only as the full, final step in the path
                            Since non-resource URLs are not namespaced, this field is only applicable for ClusterRoles referenced from a ClusterRoleBinding.
                            Rules can either apply to API resources (such as "pods" or "secrets") or non-resource URL paths (such as "/api"),  but not both.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        resourceNames:
                          description: ResourceNames is an optional white list of
                            names that the rule applies to.  An empty set means that
                            everything is allowed.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        resources:
                          description: Resources is a list of resources this rule
                            applies to. '*' represents all resources.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        verbs:
                          description: Verbs is a list of Verbs that apply to ALL
                            the ResourceKinds contained in this rule. '*' represents
                            all verbs.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - verbs
                      type: object
                    type: array
                  rules:
                    description: |-
                      Rules define namespace-scoped permissions granted to the agent's
                      ServiceAccount via a Role and RoleBinding in the agent's namespace.
                    items:
                      description: |-
                        PolicyRule holds information that describes a policy rule, but does not contain information
                        about who the rule applies to or which namespace the rule applies to.
                      properties:
                        apiGroups:
                          description: |-
                            APIGroups is the name of the APIGroup that contains the resources.  If multiple API groups are specified, any action requested against one of
                            the enumerated resources in any API group will be allowed. "" represents the core API group and "*" represents all API groups.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        nonResourceURLs:
                          description: |-
                            NonResourceURLs is a set of partial urls that a user should have access to.  *s are allowed, but only as the full, final step in the path
                            Since non-resource URLs are not namespaced, this field is only applicable for ClusterRoles referenced from a ClusterRoleBinding.
                            Rules can either apply to API resources (such as "pods" or "secrets") or non-resource URL paths (such as "/api"),  but not both.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        resourceNames:
                          description: ResourceNames is an optional white list of
                            names that the rule applies to.  An empty set means that
                            everything is allowed.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        resources:
                          description: Resources is a list of resources this rule
                            applies to. '*' represents all resources.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        verbs:
                          description: Verbs is a list of Verbs that apply to ALL
                            the ResourceKinds contained in this rule. '*' represents
                            all verbs.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - verbs
                      type: object
                    type: array
                  serviceAccountName:
                    description: |-
                      ServiceAccountName is the name of the ServiceAccount to create for this
                      agent. Defaults to "<agent-name>-sa" when omitted.
                    type: string
                type: object
              skillsConfigMap:
                description: |-
                  SkillsConfigMap is the name of a ConfigMap whose data keys are mounted
                  as files under /home/copilot/.copilot/skills/ inside the agent pod.
                type: string
              storageSize:
                default: 1Gi
                description: |-
                  StorageSize is the size of the PersistentVolumeClaim for session state.
                  Defaults to "1Gi".
                type: string
              toolServers:
                description: |-
                  ToolServers is a list of KubeCopilotToolServer names that this agent can use.
                  The operator passes these MCP server configurations to the agent container
                  as the MCP_SERVERS environment variable (JSON-encoded).
                items:
                  type: string
                type: array
            required:
            - githubTokenSecretRef
            type: object
            x-kubernetes-validations:
            - message: kubeconfigSecretRef and rbac are mutually exclusive
              rule: '!(has(self.kubeconfigSecretRef) && has(self.rbac))'
          status:
            description: KubeCopilotAgentStatus defines the observed state of KubeCopilotAgent.
            properties:
              agentID:
                description: AgentID is a stable UUID assigned on first reconcile.
                type: string
              conditions:
                description: Conditions represent the current state of the agent.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                description: 'Phase describes the current lifecycle phase: Pending,
                  Running, Error.'
                type: string
              serviceName:
                description: ServiceName is the ClusterIP service name for this agent.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
