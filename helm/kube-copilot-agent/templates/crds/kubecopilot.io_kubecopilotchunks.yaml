{{- if .Values.installCRDs }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kubecopilotchunks.kubecopilot.io
spec:
  group: kubecopilot.io
  names:
    kind: KubeCopilotChunk
    listKind: KubeCopilotChunkList
    plural: kubecopilotchunks
    singular: kubecopilotchunk
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.sendRef
      name: SendRef
      type: string
    - jsonPath: .spec.sequence
      name: Seq
      type: integer
    - jsonPath: .spec.chunkType
      name: Type
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: KubeCopilotChunk is the Schema for the kubecopilotchunks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubeCopilotChunkSpec defines a single streaming output chunk
              from the agent.
            properties:
              agentRef:
                description: AgentRef is the name of the KubeCopilotAgent that produced
                  this chunk.
                type: string
              chunkType:
                description: 'ChunkType categorises the chunk: thinking, tool_call,
                  tool_result, response, info, error.'
                type: string
              content:
                description: Content is the human-readable text for this chunk.
                type: string
              sendRef:
                description: SendRef is the name of the KubeCopilotSend that initiated
                  this stream.
                type: string
              sequence:
                description: Sequence is the ordering index of this chunk within a
                  send request.
                type: integer
              sessionID:
                description: SessionID is the conversation session this chunk belongs
                  to.
                type: string
            required:
            - agentRef
            - chunkType
            - content
            - sequence
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
{{- end }}
